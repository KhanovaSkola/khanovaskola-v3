{block content}
	{**
	 * @param Comment[] $comments
	 *}

	{control comment}

	<div n:foreach="$comments as $comment">
		<p>
			{$comment->createdAt|date}
			<strong>{$comment->author->nominative}:</strong>
		</p>
		<p>
			{if $comment->inReplyTo}
				{$comment->inReplyTo->author->vocative}:
			{/if}
			{$comment->text}
		</p>
	</div>

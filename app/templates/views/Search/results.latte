{block content}
	{**
	 * @param string $query
	 * @param Collection[] $results
	 *}

	<h1>Výsledky pro <code>{$query}</code></h1>
	{foreach $results as $content}
		<h3>
			<a n:href="$content">
				{$content->title|noescape}
			</a>
		</h3>
		<p>{$content->description|noescape}</p>
		<p>{$content->subtitles|noescape}</p>
	{/foreach}

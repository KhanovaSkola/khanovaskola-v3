#!/bin/sh
#
# Run CodeSniffer on all staged files
#

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR/../.."
git diff --name-only --cached | grep .php | xargs php "vendor/bin/phpcs" --severity=1 -p --standard="tests/cs"
